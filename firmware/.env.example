# Copy this file to firmware/.env and fill in local values.
# firmware/.env is ignored and must never be committed.
WIFI_SSID=your_wifi_name
WIFI_PASSWORD=your_wifi_password
BACKEND_URL=your-backend.example.com
DEVICE_TOKEN=your-device-token
