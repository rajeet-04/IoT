---
phase: 03-realtime-control-dashboard
plan: 04
type: execute
wave: 2
depends_on:
  - 03-02
  - 03-03
files_modified:
  - frontend/src/app/globals.css
  - frontend/src/components/dashboard/device-card.tsx
  - frontend/src/components/dashboard/device-list.tsx
  - frontend/src/components/dashboard/connection-status.tsx
autonomous: true
requirements:
  - DASH-04
must_haves:
  truths:
    - "Dashboard is usable on mobile phone screens (320px width)"
    - "Dashboard layout adapts from single column on mobile to multi-column on desktop"
    - "Toggle switches are large enough to tap on touch screens"
    - "Connection status indicator visible on all screen sizes"
  artifacts:
    - path: "frontend/src/app/globals.css"
      provides: "Responsive CSS with mobile-first breakpoints"
      contains: "@media"
    - path: "frontend/src/components/dashboard/device-card.tsx"
      provides: "Responsive device card with touch-friendly controls"
      contains: "sm:"
    - path: "frontend/src/components/dashboard/connection-status.tsx"
      provides: "Responsive connection status bar"
      exports: ["ConnectionStatus"]
  key_links:
    - from: "frontend/src/components/dashboard/device-list.tsx"
      to: "Tailwind CSS responsive classes"
      via: "grid-cols-1 md:grid-cols-2 lg:grid-cols-3"
      pattern: "grid-cols-1.*md:grid-cols"
user_setup: []
---

<objective>
Make the dashboard fully responsive with mobile-first Tailwind CSS layout, touch-friendly controls, responsive connection status indicator, and command feedback that works on all screen sizes.

Purpose: Ensure the dashboard works on mobile browsers as required by DASH-04. Without responsive design, users cannot control devices from phones — a critical use case for IoT control.
Output: Responsive CSS updates, mobile-optimized device cards, touch-friendly toggle switches, adaptive grid layout, and responsive connection status bar.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/03-realtime-control-dashboard/03-02-PLAN.md
@.planning/phases/03-realtime-control-dashboard/03-03-PLAN.md

<interfaces>
<!-- Components from Plan 03-02 and 03-03 that this plan makes responsive. -->

From frontend/src/components/dashboard/device-card.tsx (Plan 03-03):
```tsx
// Current: Card with device name, status badge, relay toggle, last seen
// Needs: responsive sizing, touch-friendly toggle, stacked layout on mobile
```

From frontend/src/components/dashboard/device-list.tsx (Plan 03-03):
```tsx
// Current: grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4
// Needs: verify mobile rendering, adjust gaps and padding for small screens
```

From frontend/src/app/dashboard/layout.tsx (Plan 03-02):
```tsx
// Current: sidebar (240px) + main content
// Needs: collapsible sidebar on mobile, hamburger menu
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement responsive dashboard layout with collapsible sidebar and mobile-first grid</name>
  <files>frontend/src/app/dashboard/layout.tsx, frontend/src/app/globals.css, frontend/src/components/dashboard/connection-status.tsx</files>
  <read_first>
    - frontend/src/app/dashboard/layout.tsx — current layout from Plan 03-02
    - frontend/src/app/globals.css — current styles from Plan 03-02
    - frontend/src/components/dashboard/sidebar.tsx — sidebar from Plan 03-02
  </read_first>
  <acceptance_criteria>
    - frontend/src/app/dashboard/layout.tsx updated with responsive layout:
      - Mobile (< 768px): sidebar hidden by default, hamburger menu button in header toggles sidebar as overlay
      - Desktop (>= 768px): sidebar always visible, no hamburger menu
      - Main content area takes full width on mobile, calc(100% - 240px) on desktop
    - Hamburger menu button: top-left of header, visible only on mobile (hidden md:hidden → visible md:hidden pattern)
    - Sidebar overlay on mobile: fixed position, full height, z-50, backdrop blur, closes on outside click
    - frontend/src/components/dashboard/connection-status.tsx extracted as separate component:
      - Renders colored dot + status text + reconnecting animation
      - Full width on mobile, compact on desktop
      - Sticky to top of content area
    - frontend/src/app/globals.css updated with:
      - Touch-friendly tap targets: button min-height 44px, input min-height 44px
      - Safe area insets for notched phones: padding env(safe-area-inset-*)
      - Smooth transitions for sidebar open/close
    - Dashboard header bar on mobile: shows hamburger + page title + connection status dot
  </acceptance_criteria>
  <action>
    1. Create frontend/src/components/dashboard/connection-status.tsx:
       - 'use client' directive
       - Import useDeviceStore
       - Get wsStatus from store
       - Render: flex items-center gap-2 px-3 py-2 rounded-lg with background color based on status
       - Connected: bg-green-50 text-green-700, green dot (animate-pulse)
       - Reconnecting: bg-yellow-50 text-yellow-700, yellow dot (animate-ping)
       - Disconnected: bg-red-50 text-red-700, red dot
       - Text: "Connected" / "Reconnecting..." / "Disconnected"
       - Full width on mobile, auto width on desktop
    2. Update frontend/src/app/dashboard/layout.tsx:
       - Add useState for sidebarOpen (mobile toggle)
       - Add hamburger button: visible on mobile (md:hidden), calls setSidebarOpen(!sidebarOpen)
       - Sidebar: on mobile, fixed inset-y-0 left-0 z-50 w-64 transform transition-transform, translate-x-0 when open, -translate-x-full when closed
       - Sidebar overlay backdrop: fixed inset-0 bg-black/20 z-40, visible when sidebarOpen on mobile, click closes sidebar
       - On desktop (md:): sidebar always visible, no overlay, no hamburger
       - Main content: ml-0 on mobile, md:ml-64 on desktop
       - Header bar on mobile: sticky top-0 z-30 bg-white border-b px-4 py-3 flex items-center gap-3
    3. Update frontend/src/app/globals.css:
       - Add touch-friendly styles:
         - button, [role="button"] { min-height: 44px; min-width: 44px; }
         - input, select, textarea { min-height: 44px; }
       - Add safe area padding:
         - body { padding-top: env(safe-area-inset-top); padding-bottom: env(safe-area-inset-bottom); }
       - Add sidebar transition:
         - .sidebar-transition { transition: transform 0.3s ease-in-out; }
    4. Update frontend/src/components/dashboard/sidebar.tsx:
       - Accept onClose prop (optional callback)
       - On mobile: close button (X) in top-right corner
       - On desktop: no close button
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Dashboard layout responsive: collapsible sidebar on mobile with hamburger menu, always-visible sidebar on desktop, connection status bar responsive, touch-friendly tap targets (44px minimum), safe area insets for notched phones</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Optimize device cards and relay toggle for mobile with touch-friendly controls</name>
  <files>frontend/src/components/dashboard/device-card.tsx, frontend/src/components/dashboard/device-list.tsx, frontend/src/components/dashboard/relay-toggle.tsx</files>
  <read_first>
    - frontend/src/components/dashboard/device-card.tsx — current card from Plan 03-03
    - frontend/src/components/dashboard/relay-toggle.tsx — current toggle from Plan 03-03
    - frontend/src/components/dashboard/device-list.tsx — current grid from Plan 03-03
  </read_first>
  <acceptance_criteria>
    - frontend/src/components/dashboard/device-card.tsx responsive:
      - Full width on mobile, auto width in grid on desktop
      - Padding: p-4 on mobile, p-6 on desktop (sm:p-6)
      - Device name: text-lg on mobile, text-xl on desktop
      - Status badge and toggle stacked vertically on mobile, side-by-side on desktop (flex-col sm:flex-row)
      - Last seen text: text-xs text-gray-500, truncated if too long
    - frontend/src/components/dashboard/relay-toggle.tsx responsive:
      - Toggle switch: w-14 h-8 on mobile, w-16 h-8 on desktop
      - Thumb: w-6 h-6 with adequate touch target
      - Label text: "ON" / "OFF" visible next to toggle
      - Disabled state: opacity-50 cursor-not-allowed
      - Loading state: spinner overlay centered in toggle
    - frontend/src/components/dashboard/device-list.tsx responsive:
      - Grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3 sm:gap-4
      - Padding: px-4 on mobile, px-6 on desktop
      - "No devices" message: centered with larger touch target for "Register" button
    - Toast notifications (sonner) positioned correctly on mobile:
      - Bottom-center on mobile, top-right on desktop
      - Full width on mobile with margin
    - All text readable on 320px width (no horizontal scrolling)
    - No overflow-x on any component
  </acceptance_criteria>
  <action>
    1. Update frontend/src/components/dashboard/device-card.tsx:
       - Card wrapper: w-full rounded-xl border bg-white shadow-sm hover:shadow-md transition-shadow
       - Padding: p-4 sm:p-6
       - Header: flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2
       - Device name: text-lg sm:text-xl font-semibold truncate
       - Edit button: p-2 (44px touch target)
       - Status + toggle section: flex flex-col sm:flex-row sm:items-center gap-3 sm:gap-4 mt-3
       - Status badge: inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-medium
       - Last seen: text-xs text-gray-500 mt-2 sm:mt-3
    2. Update frontend/src/components/dashboard/relay-toggle.tsx:
       - Toggle container: relative inline-flex items-center
       - Toggle button: relative w-14 h-8 sm:w-16 h-8 rounded-full transition-colors
       - ON state: bg-green-500, OFF state: bg-gray-300
       - Thumb: absolute left-1 top-1 w-6 h-6 bg-white rounded-full shadow transition-transform
       - ON thumb: translate-x-6 sm:translate-x-8
       - OFF thumb: translate-x-0
       - Label: ml-3 text-sm font-medium text-gray-700
       - Disabled: opacity-50 cursor-not-allowed
       - Loading: absolute inset-0 flex items-center justify-center with spinner
    3. Update frontend/src/components/dashboard/device-list.tsx:
       - Grid container: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-3 sm:gap-4 px-4 sm:px-6
       - "No devices" state: flex flex-col items-center justify-center py-12 px-4 text-center
       - "No devices" button: min-h-[44px] px-6 py-3 rounded-lg (touch-friendly)
    4. Configure sonner Toaster for mobile:
       - In dashboard page: <Toaster position="top-right" expand={true} closeButton richColors />
       - On mobile, toasts will be full-width with margin
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>All dashboard components responsive: device cards stack on mobile, toggle switches touch-friendly (44px minimum), grid adapts from 1 to 3 columns, toasts positioned correctly, no horizontal scrolling at 320px width</done>
</task>

</tasks>

<verification>
- All source files updated: frontend/src/app/dashboard/layout.tsx, frontend/src/app/globals.css, frontend/src/components/dashboard/device-card.tsx, frontend/src/components/dashboard/device-list.tsx, frontend/src/components/dashboard/relay-toggle.tsx, frontend/src/components/dashboard/connection-status.tsx
- Sidebar collapses on mobile with hamburger menu toggle
- Sidebar always visible on desktop (>= 768px)
- Device cards display in single column on mobile, 2 columns on tablet, 3 columns on desktop
- Toggle switches have minimum 44px touch targets
- All buttons and interactive elements have minimum 44px height
- Connection status bar visible and readable on all screen sizes
- Toast notifications positioned correctly on mobile and desktop
- No horizontal scrolling at 320px viewport width
- Safe area insets applied for notched phones
- Frontend builds without errors
</verification>

<success_criteria>
- Dashboard fully usable on mobile phone screens (320px minimum width)
- Sidebar accessible via hamburger menu on mobile
- Device cards stack vertically on mobile, grid on larger screens
- Relay toggle easy to tap on touch screens (44px minimum target)
- Connection status visible on all screen sizes
- Toast notifications readable on mobile
- No horizontal scrolling at any breakpoint
- Text remains readable at smallest viewport
- Dashboard layout adapts smoothly between breakpoints
- Frontend builds and deploys without CSS errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-realtime-control-dashboard/03-04-SUMMARY.md`
</output>
