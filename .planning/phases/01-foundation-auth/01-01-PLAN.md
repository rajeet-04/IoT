---
phase: 01-foundation-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - src/db/connection.js
  - src/models/User.js
  - src/models/Device.js
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - BE-05
  - DEV-01
user_setup:
  - service: mongodb-atlas
    why: "Database for user accounts and device registry"
    dashboard_config:
      - task: "Create MongoDB Atlas M0 cluster (free tier)"
        location: "https://cloud.mongodb.com -> Database -> Deploy -> Free (M0)"
      - task: "Create database user with readWrite access"
        location: "MongoDB Atlas -> Database Access -> Add New Database User"
      - task: "Whitelist IP 0.0.0.0/0 (or specific IPs) for network access"
        location: "MongoDB Atlas -> Network Access -> Add IP Address"
    env_vars:
      - name: MONGODB_URI
        source: "MongoDB Atlas -> Connect -> Connect your application -> Copy connection string"
---

<objective>
Set up MongoDB Atlas database connection with Mongoose schemas for users and devices, including validation, indexes, and connection utilities. This is the data foundation that all other Phase 1 plans depend on.

Purpose: Provide the database layer that auth endpoints and device registration will use.
Output: Mongoose models, connection module, environment configuration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-auth/01-CONTEXT.md
@.planning/research/STACK.md

<interfaces>
<!-- No existing code — greenfield project. These are the contracts to create. -->

From src/models/User.js (to create):
```javascript
// User document shape
{
  _id: ObjectId,
  email: string (unique, lowercase, required),
  passwordHash: string (required, min 8 chars after hashing),
  createdAt: Date (auto),
  updatedAt: Date (auto)
}
```

From src/models/Device.js (to create):
```javascript
// Device document shape
{
  _id: ObjectId,
  userId: ObjectId (ref: User, required),
  deviceId: string (unique UUID, required),
  name: string (default: "Device-{deviceId}"),
  status: string (enum: ["offline", "online"], default: "offline"),
  lastSeen: Date,
  createdAt: Date (auto),
  updatedAt: Date (auto)
}
```

From src/db/connection.js (to create):
```javascript
// Exports
export async function connectDB(uri) // Returns Mongoose instance
export function getDB() // Returns cached Mongoose instance or throws
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initialize project with Express 5, Mongoose 8, zod, bcrypt, jsonwebtoken, and dev dependencies</name>
  <files>package.json, .env.example, .gitignore</files>
  <read_first>
    - .planning/research/STACK.md — exact versions and installation commands
    - .planning/phases/01-foundation-auth/01-CONTEXT.md — locked decisions D-01 through D-14
  </read_first>
  <behavior>
    - Test: `node --version` returns v22.x or compatible
    - Test: `npm ls express mongoose zod bcrypt jsonwebtoken` shows all installed
    - Test: `npm ls nodemon jest` shows dev dependencies installed
    - Test: .env.example contains MONGODB_URI=, JWT_SECRET=, JWT_REFRESH_SECRET=, PORT=
    - Test: .gitignore contains node_modules/, .env, *.log
  </behavior>
  <action>
    1. Run `npm init -y` to create package.json
    2. Set package.json: `"type": "module"`, `"engines": { "node": ">=22.0.0" }`, `"scripts": { "dev": "nodemon src/index.js", "start": "node src/index.js", "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js" }`
    3. Install production dependencies: `npm install express@5 mongoose@8 zod bcrypt jsonwebtoken dotenv`
    4. Install dev dependencies: `npm install -D nodemon jest`
    5. Create .env.example with: MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/iot-control?retryWrites=true&w=majority, JWT_SECRET=change-me-to-random-string, JWT_REFRESH_SECRET=change-me-to-random-string-too, PORT=3000
    6. Create .gitignore with: node_modules/, .env, *.log, coverage/, .DS_Store
    7. Create src/ directory structure: src/db/, src/models/, src/routes/, src/middleware/, src/utils/
  </action>
  <verify>
    <automated>npm ls express mongoose zod bcrypt jsonwebtoken --depth=0 2>&1 | grep -c "deduped\|valid" | xargs test 5 -eq</automated>
  </verify>
  <done>All dependencies installed, package.json configured with ESM and scripts, .env.example and .gitignore created, src/ directory structure exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create MongoDB connection module with Mongoose, User model, and Device model with schema validation and indexes</name>
  <files>src/db/connection.js, src/models/User.js, src/models/Device.js</files>
  <read_first>
    - src/db/connection.js (created by Task 1 skeleton — read current state)
    - .planning/research/STACK.md — MongoDB driver v7.1.x, Mongoose 8.x rationale
    - .planning/phases/01-foundation-auth/01-CONTEXT.md — decisions D-08, D-09, D-10, D-11
  </read_first>
  <behavior>
    - Test: connectDB() with valid URI resolves without error
    - Test: connectDB() with invalid URI rejects with connection error
    - Test: getDB() throws if called before connectDB()
    - Test: User.create({email, passwordHash}) succeeds with valid data
    - Test: User.create({email: "duplicate"}) fails with duplicate key error
    - Test: User.create({email: "no-at"}) fails validation
    - Test: Device.create({userId, deviceId}) succeeds
    - Test: Device.create({userId, deviceId: "duplicate"}) fails with duplicate key error
    - Test: User schema has index on email (unique)
    - Test: Device schema has index on deviceId (unique) and userId
  </behavior>
  <action>
    1. Create src/db/connection.js:
       - Import mongoose from 'mongoose'
       - Module-level cache variable `_connection` initialized to null
       - Export async function connectDB(uri): if _connection already exists return it, else call mongoose.connect(uri) with options { serverApi: { version: '1' } }, store result in _connection, log "MongoDB connected", return _connection
       - Export function getDB(): if !_connection throw new Error("Database not connected — call connectDB() first"), return _connection
       - Add mongoose connection event listeners: 'error' logs error, 'disconnected' logs warning
    2. Create src/models/User.js:
       - Import mongoose from 'mongoose'
       - Define userSchema with: email { type: String, required: true, unique: true, lowercase: true, trim: true, match: [/^\S+@\S+\.\S+$/, 'Please enter a valid email'] }, passwordHash { type: String, required: true, minlength: [8, 'Password must be at least 8 characters'] }, createdAt { type: Date, default: Date.now }, updatedAt { type: Date, default: Date.now }
       - Add pre('save') middleware: set updatedAt to Date.now on every save
       - Export default mongoose.model('User', userSchema)
    3. Create src/models/Device.js:
       - Import mongoose from 'mongoose'
       - Define deviceSchema with: userId { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true, index: true }, deviceId { type: String, required: true, unique: true, trim: true }, name { type: String, default: function() { return `Device-${this.deviceId?.slice(0, 8) || 'unknown'}` } }, status { type: String, enum: ['offline', 'online'], default: 'offline' }, lastSeen { type: Date }, createdAt { type: Date, default: Date.now }, updatedAt { type: Date, default: Date.now }
       - Add pre('save') middleware: set updatedAt to Date.now on every save
       - Export default mongoose.model('Device', deviceSchema)
  </action>
  <verify>
    <automated>node --experimental-vm-modules node_modules/jest/bin/jest.js tests/models.test.js --testPathPattern="models" 2>&1 | tail -5</automated>
  </verify>
  <done>User and Device Mongoose models created with schema validation, unique indexes on email and deviceId, userId index on Device, createdAt/updatedAt timestamps, and connection module with caching</done>
</task>

</tasks>

<verification>
- All source files exist: src/db/connection.js, src/models/User.js, src/models/Device.js
- package.json has all required dependencies
- .env.example contains all 4 required environment variables
- Mongoose schemas enforce email uniqueness, deviceId uniqueness, email format validation
- Connection module caches MongoDB connection (single instance pattern)
</verification>

<success_criteria>
- MongoDB Atlas M0 cluster provisioned and connection string in .env
- User model enforces unique email, valid email format, passwordHash required
- Device model enforces unique deviceId, references User, defaults status to "offline"
- Connection module exports connectDB(uri) and getDB() with proper error handling
- All tests pass for model validation and index creation
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-auth/01-01-SUMMARY.md`
</output>
