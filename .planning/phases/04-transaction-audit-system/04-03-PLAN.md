---
phase: 04-transaction-audit-system
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
  - 03-02
  - 03-03
files_modified:
  - frontend/src/app/dashboard/transactions/page.tsx
  - frontend/src/app/dashboard/layout.tsx
  - frontend/src/components/transactions/transaction-table.tsx
  - frontend/src/components/transactions/transaction-summary.tsx
  - frontend/src/components/transactions/chain-integrity.tsx
  - frontend/src/lib/api.ts
autonomous: true
requirements:
  - TXN-04
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - "User can view transaction history in a sortable table with timestamps"
    - "Dashboard displays total ON duration metrics for the device"
    - "Chain integrity indicator visible in the UI"
    - "Transaction table paginated — never loads all records at once"
  artifacts:
    - path: "frontend/src/app/dashboard/transactions/page.tsx"
      provides: "Transaction history page with table, summary, and integrity check"
      exports: ["default"]
      contains: "TransactionTable"
    - path: "frontend/src/components/transactions/transaction-table.tsx"
      provides: "Paginated, sortable transaction data table"
      exports: ["TransactionTable"]
      contains: "TransactionTable"
    - path: "frontend/src/components/transactions/transaction-summary.tsx"
      provides: "Duration metrics display (total ON time, avg duration, count)"
      exports: ["TransactionSummary"]
      contains: "TransactionSummary"
    - path: "frontend/src/components/transactions/chain-integrity.tsx"
      provides: "Chain integrity status indicator"
      exports: ["ChainIntegrity"]
      contains: "ChainIntegrity"
  key_links:
    - from: "frontend/src/components/transactions/transaction-table.tsx"
      to: "/api/transactions/:deviceId/history"
      via: "apiGet with pagination params"
      pattern: "apiGet.*transactions.*history"
    - from: "frontend/src/components/transactions/transaction-summary.tsx"
      to: "/api/transactions/:deviceId/summary"
      via: "apiGet for duration metrics"
      pattern: "apiGet.*transactions.*summary"
user_setup: []
---

<objective>
Build the transaction history UI with a paginated data table, duration metrics display, chain integrity indicator, and navigation integration into the dashboard sidebar.

Purpose: Give users visibility into their device's action history with the blockchain-style audit trail. This completes the audit system by making the immutable log accessible and understandable.
Output: Transaction history page with sortable/paginated table, duration summary cards, chain integrity badge, and sidebar navigation link.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-realtime-control-dashboard/03-02-PLAN.md
@.planning/phases/04-transaction-audit-system/04-02-PLAN.md

<interfaces>
<!-- Key contracts from Plan 04-02 backend API that this plan consumes. -->

From backend transaction API (Plan 04-02):
```javascript
// GET /api/transactions/:deviceId/history?limit=50&skip=0&sort=desc
// Returns: { transactions: [{index, action, timestamp, duration, hash, prevHash}], total, limit, skip, hasMore }

// GET /api/transactions/:deviceId/summary
// Returns: { deviceId, totalTransactions, totalOnDuration, avgOnDuration, lastAction, lastActionTime }

// GET /api/transactions/chain-integrity
// Returns: { valid: boolean, checkedAt: string, firstInvalidIndex: number | null }
```

From frontend/src/lib/api.ts (Plan 03-02):
```typescript
export async function apiGet(path: string): Promise<Response>
// Prefixes with NEXT_PUBLIC_BACKEND_URL, includes credentials: 'include'
```

Contracts this plan creates:
```typescript
// frontend/src/components/transactions/transaction-table.tsx:
// Props: { deviceId: string }
// Fetches paginated history, renders table with columns: Index, Action, Timestamp, Duration, Hash (truncated)
// Pagination controls: Previous/Next buttons, page info
// Sort toggle: newest first (default) or oldest first

// frontend/src/components/transactions/transaction-summary.tsx:
// Props: { deviceId: string }
// Renders cards: Total Transactions, Total ON Time, Avg ON Duration, Last Action

// frontend/src/components/transactions/chain-integrity.tsx:
// Props: { deviceId: string }
// Renders badge: "Chain Valid" (green) or "Chain Broken" (red) with timestamp
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create transaction table component with pagination, sorting, and hash display</name>
  <files>frontend/src/components/transactions/transaction-table.tsx, frontend/src/app/dashboard/transactions/page.tsx</files>
  <read_first>
    - frontend/src/lib/api.ts — apiGet from Plan 03-02
    - .planning/phases/04-transaction-audit-system/04-02-PLAN.md — API response format
  </read_first>
  <acceptance_criteria>
    - frontend/src/components/transactions/transaction-table.tsx is a client component
    - Props: { deviceId: string }
    - Fetches transactions from GET /api/transactions/${deviceId}/history?limit=${limit}&skip=${skip}&sort=${sort}
    - Renders HTML table with columns: # (index), Action, Timestamp, Duration, Hash (truncated to 16 chars + "...")
    - Action column: shows "ON" in green text or "OFF" in red text
    - Timestamp column: formatted as relative time (e.g., "2 hours ago") + full ISO on hover (title attribute)
    - Duration column: formatted as human-readable (e.g., "5m 23s" or "1h 12m 5s")
    - Hash column: truncated to first 16 chars + "..." with full hash in title attribute for tooltip
    - Pagination controls at bottom:
      - "Previous" button (disabled when skip === 0)
      - "Next" button (disabled when !hasMore)
      - Page info: "Showing {skip+1}-{skip+limit} of {total}"
    - Sort toggle button: "Newest first" / "Oldest first" (toggles sort between 'desc' and 'asc')
    - Loading state: skeleton rows or "Loading..." text while fetching
    - Empty state: "No transactions recorded yet" when total === 0
    - Error state: "Failed to load transactions" with retry button on fetch error
    - Default limit: 20 rows per page
  </acceptance_criteria>
  <action>
    1. Create frontend/src/components/transactions/transaction-table.tsx:
       - 'use client' directive
       - Import useState, useEffect, useCallback from react
       - Import { apiGet } from '../../lib/api'
       - State: transactions ([]), total (0), skip (0), limit (20), sort ('desc'), loading (true), error (null)
       - useEffect: fetchTransactions() on mount and when skip/sort/deviceId changes
       - fetchTransactions():
         - setLoading(true), setError(null)
         - try: const res = await apiGet(`/api/transactions/${deviceId}/history?limit=${limit}&skip=${skip}&sort=${sort}`); const data = await res.json(); setTransactions(data.transactions); setTotal(data.total); setHasMore(data.hasMore)
         - catch: setError(err.message)
         - finally: setLoading(false)
       - Render:
         - If loading: render 5 skeleton rows (gray animated bars)
         - If error: render error message with "Retry" button (calls fetchTransactions)
         - If total === 0: render "No transactions recorded yet" centered text
         - If transactions.length > 0: render table
       - Table structure:
         - thead: #, Action, Timestamp, Duration, Hash
         - tbody: map transactions to rows
         - Each row: index, action (colored), timestamp (relative + title), duration (formatted), hash (truncated + title)
       - Pagination: flex container with Previous button, page info text, Next button
       - Sort toggle: button above table showing current sort direction
    2. Create frontend/src/app/dashboard/transactions/page.tsx:
       - 'use client' directive
       - Accepts searchParams for deviceId (from URL query)
       - If no deviceId: show "Select a device to view transactions" with device selector dropdown
       - Device selector: fetches GET /api/devices, renders dropdown with device names
       - When deviceId selected: renders TransactionTable and TransactionSummary components
       - Page title: "Transaction History"
       - Breadcrumb: Dashboard > Transaction History
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <done>Transaction table renders paginated history with sortable columns, formatted timestamps, human-readable durations, truncated hashes with full tooltip, loading/empty/error states, builds without TypeScript errors</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create transaction summary cards, chain integrity indicator, and sidebar navigation link</name>
  <files>frontend/src/components/transactions/transaction-summary.tsx, frontend/src/components/transactions/chain-integrity.tsx, frontend/src/app/dashboard/layout.tsx, frontend/src/components/dashboard/sidebar.tsx</files>
  <read_first>
    - frontend/src/components/dashboard/sidebar.tsx — current sidebar from Plan 03-02
    - frontend/src/app/dashboard/layout.tsx — current layout from Plan 03-02
    - .planning/phases/04-transaction-audit-system/04-02-PLAN.md — summary and chain integrity API responses
  </read_first>
  <acceptance_criteria>
    - frontend/src/components/transactions/transaction-summary.tsx is a client component
    - Props: { deviceId: string }
    - Fetches GET /api/transactions/${deviceId}/summary
    - Renders 4 stat cards in a responsive grid (grid-cols-2 lg:grid-cols-4):
      - "Total Transactions" — number with icon
      - "Total ON Time" — formatted duration (e.g., "3h 45m 12s")
      - "Avg ON Duration" — formatted duration (e.g., "12m 34s")
      - "Last Action" — action type + relative time (e.g., "ON — 5 minutes ago")
    - Each card: white background, rounded corners, shadow, padding, label + value
    - Loading state: skeleton cards while fetching
    - Error state: silent failure (no display) — summary is non-critical
    - frontend/src/components/transactions/chain-integrity.tsx is a client component
    - Props: { deviceId: string }
    - Fetches GET /api/transactions/chain-integrity
    - Renders badge:
      - Valid: green checkmark icon + "Chain Valid" + "Verified at {timestamp}"
      - Invalid: red warning icon + "Chain Integrity Compromised" + "First broken entry: #{index}"
      - Loading: gray "Verifying..." text
    - Badge positioned at top of transaction history page
    - frontend/src/components/dashboard/sidebar.tsx updated:
      - "Transaction Log" link enabled (was placeholder in Plan 03-02)
      - Links to /dashboard/transactions
      - Active state when on transactions page
    - frontend/src/app/dashboard/layout.tsx updated if needed for transactions route
  </acceptance_criteria>
  <action>
    1. Create frontend/src/components/transactions/transaction-summary.tsx:
       - 'use client' directive
       - Import useState, useEffect from react
       - Import { apiGet } from '../../lib/api'
       - State: summary (null), loading (true)
       - useEffect: fetch summary on mount and when deviceId changes
       - fetchSummary(): try { const res = await apiGet(`/api/transactions/${deviceId}/summary`); setSummary(await res.json()) } catch { /* silent */ } finally { setLoading(false) }
       - Helper function formatDuration(seconds): 
         - If seconds < 60: `${seconds}s`
         - If seconds < 3600: `${Math.floor(seconds/60)}m ${seconds%60}s`
         - Else: `${Math.floor(seconds/3600)}h ${Math.floor((seconds%3600)/60)}m ${seconds%60}s`
       - Render: grid grid-cols-2 lg:grid-cols-4 gap-4
       - Each card: div with p-4 rounded-lg border bg-white shadow-sm
       - Card contents: label (text-sm text-gray-500), value (text-2xl font-bold)
       - If loading: render skeleton (animate-pulse gray bars)
       - If !summary: render nothing (silent failure)
    2. Create frontend/src/components/transactions/chain-integrity.tsx:
       - 'use client' directive
       - Import useState, useEffect from react
       - Import { apiGet } from '../../lib/api'
       - State: integrity (null), loading (true)
       - useEffect: fetch integrity on mount
       - fetchIntegrity(): try { const res = await apiGet('/api/transactions/chain-integrity'); setIntegrity(await res.json()) } catch { /* silent */ } finally { setLoading(false) }
       - Render:
         - If loading: "Verifying chain integrity..." with spinner
         - If integrity.valid: green badge with CheckCircle icon, "Chain Valid", "Verified at {new Date(integrity.checkedAt).toLocaleString()}"
         - If !integrity.valid: red badge with AlertTriangle icon, "Chain Integrity Compromised", "First broken entry: #{integrity.firstInvalidIndex}"
       - Styling: flex items-center gap-2 p-3 rounded-lg with appropriate background color
    3. Update frontend/src/components/dashboard/sidebar.tsx:
       - Change "Transaction Log" from disabled/placeholder to active link
       - Link href: "/dashboard/transactions"
       - Add active state detection: usePathname() from next/navigation, highlight when pathname starts with /dashboard/transactions
    4. Update frontend/src/app/dashboard/layout.tsx if needed:
       - Ensure /dashboard/transactions route is accessible within dashboard layout
       - No changes needed if using nested App Router structure
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Transaction summary cards display duration metrics with human-readable formatting, chain integrity badge shows valid/invalid status with verification timestamp, sidebar navigation link to transaction log enabled, all components build without errors</done>
</task>

</tasks>

<verification>
- All source files exist: frontend/src/app/dashboard/transactions/page.tsx, frontend/src/components/transactions/transaction-table.tsx, frontend/src/components/transactions/transaction-summary.tsx, frontend/src/components/transactions/chain-integrity.tsx
- Transaction table renders paginated data with 20 rows per page
- Table columns: Index, Action (colored), Timestamp (relative), Duration (formatted), Hash (truncated)
- Pagination controls: Previous/Next buttons with page info
- Sort toggle: newest first / oldest first
- Loading, empty, and error states all handled
- Summary cards display: total transactions, total ON time, avg ON duration, last action
- Duration formatted as human-readable (h/m/s)
- Chain integrity badge shows green (valid) or red (invalid) with timestamp
- Sidebar "Transaction Log" link enabled and routes to /dashboard/transactions
- Transaction history page has device selector for multi-device support
- Frontend builds without TypeScript errors
</verification>

<success_criteria>
- Users can view transaction history in a paginated table (20 rows per page)
- Table columns sortable: newest/oldest first toggle
- Timestamps displayed as relative time with full ISO on hover
- Durations formatted as human-readable (e.g., "5m 23s", "1h 12m")
- Hash values truncated to 16 chars with full hash visible on hover
- Summary cards show total transactions, total ON time, average duration, last action
- Chain integrity badge visible at top of transaction history page
- Green badge when chain is valid, red when compromised
- Sidebar navigation includes working "Transaction Log" link
- Device selector allows viewing transactions for different devices
- Transaction table never loads all records at once (paginated API)
- Frontend builds and deploys without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-transaction-audit-system/04-03-SUMMARY.md`
</output>
